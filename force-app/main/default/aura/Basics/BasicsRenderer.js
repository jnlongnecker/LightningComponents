/*
    Our final JS file (also not a controller) is the Renderer. This holds overrides to methods written that take place on 
    certain rendering events for the given component.
*/

({

    // Your renderer method overrides go here

})
